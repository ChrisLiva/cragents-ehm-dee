# cragents-ehm-dee

Templated, git-versioned management of user-level agent instructions:
`~/.claude/CLAUDE.md` and `~/.codex/AGENTS.md`.

This first commit is a pure pre-tool snapshot of both destinations as they exist
today, drift and all. The tooling arrives in the next commit.
